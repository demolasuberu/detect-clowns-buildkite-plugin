steps:
  - label: "🤡 :hammer:"
    plugins:
      - plugin-tester#v2.0.0: ~

  - label: ":sparkles: lint"
    plugins:
      - plugin-linter#v3.3.0:
          id: detect-clowns

  - label: ":shell: Shellcheck"
    plugins:
      - shellcheck#v1.4.0:
          files: hooks/**
