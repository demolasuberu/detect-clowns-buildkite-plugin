steps:
  - label: "🤡 :hammer:"
    plugins:
      - plugin-tester#v2.0.0: ~

  - label: ":sparkles: lint"
    command: "bk run plugin-linter || echo 'Linting step finished.'"
    plugins:
      - plugin-linter#v3.3.0:
          id: detect-clowns

  - label: ":shell: Shellcheck"
    command: "shellcheck hooks/** || echo 'shellcheck step finished.'"
    plugins:
      - shellcheck#v1.4.0:
          files: hooks/**
